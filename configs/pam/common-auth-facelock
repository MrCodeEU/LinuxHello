# PAM configuration for FaceLock
# Add this to your PAM configuration to enable face authentication
#
# Example usage in /etc/pam.d/common-auth (Debian/Ubuntu) or
# /etc/pam.d/system-auth (Fedora/RHEL):
#
#   auth    sufficient      pam_facelock.so
#   auth    required        pam_unix.so try_first_pass
#
# Or for sudo:
#
#   auth    sufficient      pam_facelock.so
#   auth    include         system-auth

# FaceLock authentication module
# Place this BEFORE other authentication methods to try face first
auth    sufficient      pam_facelock.so

# Fallback to standard Unix authentication
auth    required        pam_unix.so try_first_pass nullok
